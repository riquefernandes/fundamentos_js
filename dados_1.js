let preco = 19.90
let desconto = 0.4

console.log(preco * (1 - desconto))

// Podemos melhorar o entendimento criando mais uma variável

let precoComDesconto = preco * (1 - desconto)

console.log(precoComDesconto)

// Sempre que possível armazenar algum valor que precise ser replicado e ou reutilizado, guarde esse valor em uma variável